import { FC } from "react";
import { Typography } from "@mui/material";

const IntervalTimerPage: FC = () => {
  return (
    <div>
      <Typography variant="h6" component="h6" sx={{ fontStyle: "italic" }}>
        {"Coming Soon..."}
      </Typography>
    </div>
  );
};

export default IntervalTimerPage;
